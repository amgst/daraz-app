engine-strict=true
@shopify:registry=https://registry.npmjs.org
# @vercel/remix pins an exact @remix-run/dev peer version, but our own
# @remix-run/* deps float on ^2.16.1 - that drifts out of sync as new 2.x
# patches ship, so peer resolution has to be non-fatal.
legacy-peer-deps=true
